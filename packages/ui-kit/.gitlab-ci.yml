image: node:18.14

stages:
  - build
  - type_check
  - lint

build:
  stage: build
  cache:
    - key: $CI_COMMIT_REF_NAME
      policy: pull-push
      paths:
        - node_modules
  script:
    - npm run build --workspace=@rosen-bridge/ui-kit

type_check:
  stage: type_check
  cache:
    key: $CI_COMMIT_REF_NAME
    policy: pull
    paths:
      - node_modules
  script:
    - npm run type-check --workspace=@rosen-bridge/ui-kit

lint:
  stage: lint
  cache:
    key: $CI_COMMIT_REF_NAME
    policy: pull
    paths:
      - node_modules
  script:
    - npm run lint --workspace=@rosen-bridge/ui-kit
