import {
  createTheme,
  darkThemeOptions,
  lightThemeOptions,
} from '@rosen-bridge/ui-kit';

export const theme = {
  light: createTheme(lightThemeOptions),
  dark: createTheme(darkThemeOptions),
};
