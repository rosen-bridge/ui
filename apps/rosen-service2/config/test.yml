redis:
  address: http://kv
  token: 'kv-token'
chains:
  binance:
    adapter:
      chunkSize: 1
  ethereum:
    adapter:
      chunkSize: 1

paths:
  tokens: tests/config/tokensMap.json
  contracts: tests/config/contracts.json
