---
"@rosen-bridge/rosen-service2": minor
---

Add `BridgeAmountMetricService` to handle bridge amount metrics calculation
Add `BridgeFeeMetricService` to handle bridge fee metrics calculation
Add `WatcherCountMetricService` to handle watcher count metrics calculation
