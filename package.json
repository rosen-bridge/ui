{
  "name": "rosen-bridge",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "wallets/*"
  ],
  "scripts": {
    "prepare": "husky install",
    "test": "vitest",
    "test-and-exit": "vitest --run",
    "version": "npx changeset version && npm i"
  },
  "dependencies": {
    "@changesets/cli": "^2.27.1"
  },
  "devDependencies": {
    "@rosen-bridge/changeset-formatter": "^0.1.0",
    "husky": "^8.0.0",
    "lint-staged": "^13.2.2",
    "vitest": "^1.2.2",
    "prettier": "^2.8.8"
  }
}
