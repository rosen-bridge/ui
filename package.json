{
  "name": "rosen-bridge",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky install",
    "type-check": "npm run type-check --workspaces",
    "prettify": "npm run prettify --workspaces",
    "lint": "npm run prettify --workspaces"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^13.2.2",
    "prettier": "^2.8.8",
    "typescript": "^5.0.2"
  }
}